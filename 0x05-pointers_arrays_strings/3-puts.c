#include "main.h"

/**
 * _puts - prints a string
 *@str: a string parameter to array
 * Return: Always 0.
 */

void _puts(char *str)
{
	int i;
	int s[i];
	int length = 0;

	for (i = 0; s[i] != '\0'; i++)
	{
		length++;
	}
	return (length);
}
